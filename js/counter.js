"use strict"
/*window.addEventListener('click',function(event){
const cardCounter=event.target.closest('.card__counter')
const counterPrice=cardCounter.querySelectorAll('.card__counter-number')/*
       if(event.target.dataset.action==='minus'){
        if(parseInt(counterPrice.innerText)>1){
        counterPrice.innerText=--counterPrice.innerText        }
    }
        if(event.target.dataset.action==='plus'){
        counterPrice.innerText=++counterPrice.innerText
        }
})*/
